#include <bits/stdc++.h>
using namespace std;

int main(){
	int in[5];
	scanf("%d,%d,%d,%d,%d", &in[0], &in[1], &in[2], &in[3], &in[4]);
	printf("%d %d %d %d %d\n", in[4], in[3], in[2], in[1], in[0]);
	return 0;
}
