#include <bits/stdc++.h>
using namespace std;

int main(){
	int in, sum = 0;
	cin >> in;
	sum += in;
	cin >> in;
	cin >> in;
	
	cin >> in;
	cin >> in;
	sum += in;
	cin >> in;
	
	cin >> in;
	cin >> in;
	cin >> in;
	sum += in;
	
	cout << sum << endl;
	return 0;
}
